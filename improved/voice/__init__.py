"""voice -- čistý unifikovaný generátor (viz ../CLEAN_GENERATOR_SPEC.md).

Fáze stavby:
  ① SPEC (hotovo)
  ② harmony + render + triviální generátor (slyšet harmonii)   <-- TADY
  ③ melody: jeden kontextově podmíněný model (Evans prior, scale-degree)
  ④ feedback (volný drift)
  ⑤ slim GUI
"""
