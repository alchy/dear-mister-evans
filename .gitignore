# Python
.venv/
__pycache__/
*.pyc
*.pyo

# generované výstupy (MIDI)
outputs_*/
gallery/
concept/evans_melody_gen/outputs/

# trénovací data (přepisy B. Evanse – autorská práva, nepublikovat)
concept/evans_melody_gen/data/*.mid
concept/evans_melody_gen/data/*.midi

# externí stažená data + natrénované modely (lokální, mimo repo)
data_external/
models/

# pomocné/scratch (screenshot demo)
_shot.ps1
pianochord_shot.png

# OS
.DS_Store
Thumbs.db

# GUI stav (lokální konfigurace) + feedback uživatele (preference k ladění)
jazz_gui_state.json
jazz_feedback.json
